# 10-Second Clicker

Click as many times as you can in 10 seconds. The best score is saved in your browser.

## Play locally

Open `index.html` directly (file://) or run a quick static server:

```bash
python3 -m http.server 8000
```

Then visit `http://localhost:8000`.
